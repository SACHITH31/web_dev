// == (double equals to)
// this won't checks whether the 2 variables are of having same data types instead they check if the both the values are same then it does implicit conversion


// === (triple equals to)
// this first checks whether those 2 variables are of same data type. if yes then they check values, else it returns false
// IT DOES STRICTLY EQUALITY